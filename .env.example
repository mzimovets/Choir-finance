# Обязательно: секрет для подписи JWT-токенов (минимум 32 символа)
# Сгенерировать: openssl rand -base64 32
JWT_SECRET=

# Опционально: путь к директории с базой данных (по умолчанию ./data)
# DATA_DIR=/var/data/chorus
